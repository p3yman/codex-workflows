# Issue Reference

Use this file to record project-specific issue routing and taxonomy for Codex Workflows. Keep it current enough that agents can create and refine issues without guessing labels, projects, statuses, or assignees.

## Issue Systems

- Primary system: `{linear|github|jira|other_or_none}`
- Workspace or organization: `{workspace_or_org_name_or_none}`
- Default team or board: `{team_or_board_name_or_none}`
- Default project: `{project_name_or_none}`

## Boards and Teams

| Board or Team | When to Use | Default Status | Notes |
|---|---|---|---|
| `{board_or_team}` | `{routing_rule}` | `{status}` | `{notes}` |

## Projects

| Project | Key or ID | When to Use | Owner | Notes |
|---|---|---|---|---|
| `{project_name}` | `{key_or_id}` | `{routing_rule}` | `{owner_or_none}` | `{notes}` |

## Issue Codes

| Prefix or Code | Meaning | Example | Notes |
|---|---|---|---|
| `{ABC}` | `{meaning}` | `{ABC-123}` | `{notes}` |

## Labels

| Label | When to Apply | Avoid When | Notes |
|---|---|---|---|
| `{label}` | `{usage_rule}` | `{avoid_rule_or_none}` | `{notes}` |

## Statuses

| Status | Meaning | Who Can Set | Notes |
|---|---|---|---|
| `{status}` | `{meaning}` | `{role_or_anyone}` | `{notes}` |

## Users and Ownership

| User or Role | Handle or ID | Owns | Assign When | Notes |
|---|---|---|---|---|
| `{name_or_role}` | `{handle_or_id}` | `{area}` | `{assignment_rule}` | `{notes}` |

## Routing Rules

- Bugs: `{bug_routing_rule_or_none}`
- Features: `{feature_routing_rule_or_none}`
- Chores: `{chore_routing_rule_or_none}`
- Docs: `{docs_routing_rule_or_none}`
- UX or redesign: `{ux_routing_rule_or_none}`
- Research or spikes: `{research_routing_rule_or_none}`

## Creation Defaults

- Default issue type: `{default_type_or_none}`
- Default size: `{default_size_or_none}`
- Default labels: `{default_labels_or_none}`
- Default assignee: `{default_assignee_or_none}`
- Default subscribers or watchers: `{default_watchers_or_none}`

## Examples

| Situation | Project | Labels | Assignee | Notes |
|---|---|---|---|---|
| `{example_situation}` | `{project}` | `{labels}` | `{assignee}` | `{notes}` |

## Unknowns

- `{unknown_or_none}`
